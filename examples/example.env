# Four.meme Sniper Bot - Example Environment Configuration
# Copy this file to .env and fill in your values

# ============================================
# BSC RPC Configuration
# ============================================
# Public RPC endpoint (default provided)
# For better performance, consider using a private RPC endpoint
RPC_URL=https://bsc-dataseed1.binance.org/

# Alternative RPC endpoints:
# RPC_URL=https://bsc-dataseed2.binance.org/
# RPC_URL=https://bsc-dataseed3.binance.org/
# RPC_URL=https://bsc-dataseed4.binance.org/
# RPC_URL=https://bsc-dataseed.binance.org/

# ============================================
# Wallet Configuration (Required for auto-buy)
# ============================================
# Your wallet private key (without 0x prefix)
# ⚠️  SECURITY WARNING: Never share this key!
PRIVATE_KEY=your_private_key_here_without_0x_prefix

# Your wallet address (with 0x prefix)
WALLET_ADDRESS=0xYourWalletAddressHere

# ============================================
# Auto-Buy Configuration
# ============================================
# Enable/disable automatic token purchasing
# Set to 'true' to enable, 'false' to disable
AUTO_BUY_ENABLED=true

# Amount of BNB to spend per token purchase
# Recommended: Start with small amounts (0.001-0.01 BNB)
BUY_AMOUNT_BNB=0.001

# Gas price in Gwei for transactions
# Recommended values:
#   - Normal: 3-5 Gwei (economical, 3-5 second confirmation)
#   - Fast: 5-8 Gwei (priority, 1-3 second confirmation)
#   - Urgent: 10+ Gwei (highest priority, sub-second confirmation)
BUY_GAS_PRICE_GWEI=3

# ============================================
# Performance Configuration
# ============================================
# Performance mode selection
# Options: default, high, ultra
#   - default: Up to 1,000 tokens/hour (10K tx cache)
#   - high: Up to 10,000 tokens/hour (50K tx cache)
#   - ultra: 10,000+ tokens/hour (100K tx cache)
TOKEN_VOLUME=default

# ============================================
# Notes
# ============================================
# 1. Always test with small amounts first
# 2. Keep sufficient BNB for gas fees (0.001-0.01 BNB per tx)
# 3. Monitor gas prices on BSCScan before running
# 4. Use a dedicated trading wallet (not your main wallet)
# 5. Never commit your .env file to version control
